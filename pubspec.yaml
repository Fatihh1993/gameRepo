name: code_quiz_game
description: Modern bir programlama quiz oyunu
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  google_fonts: ^6.1.0
  cloud_firestore: ^5.0.0
  firebase_core: ^3.0.0
  firebase_auth: ^5.0.0
  flutter_animate: ^4.3.0
  flutter_highlight: ^0.7.0  # Syntax highlighting için

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  # assets:
  #   - assets/images/
